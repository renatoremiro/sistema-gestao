import { config, state } from './state.js';
import { openEditModal } from './ui.js';
import { editEvent } from './handlers.js';

export { config, state, openEditModal, editEvent };

export default {
    config,
    state,
    openEditModal,
    editEvent
};
